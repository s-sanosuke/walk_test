using UnityEngine;
using System.Collections;

public class checkEvent : MonoBehaviour {

	public Camera targetCameara;
	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
//		transform.position.y (targetCameara.transform.position);
	}
}
